{
  "name": "exchange-wallet-dapp",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "check": "cd contracts && forge build && forge test && cd ../backend && npm run build && npm run test && cd ../frontend && npm run build && npm run lint"
  }
}
